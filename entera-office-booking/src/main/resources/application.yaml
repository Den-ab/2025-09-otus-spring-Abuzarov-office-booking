spring:
  application:
    name: entera-office-booking
  datasource:
    url: jdbc:postgresql://localhost:5432/entera_office_booking?characterEncoding=UTF8
    username: ------
    password: ------
    driver-class-name: org.postgresql.Driver
  flyway:
    schemas: public
    locations: classpath:db/migration
logging:
  level:
    org.flywaydb: debug
